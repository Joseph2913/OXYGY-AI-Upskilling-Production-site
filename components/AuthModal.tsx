import React from 'react';

/**
 * Auth removed — AuthModal is now a no-op.
 * Kept as an empty component so existing imports don't break.
 */
export const AuthModal: React.FC<{ onClose?: () => void }> = () => null;
